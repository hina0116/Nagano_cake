<h2>ジャンル編集</h2>
　<%= form_with model: @genres, url: admin_genre_path(@genre.id), method: :patch do |f| %>

    <h6>ジャンル名
      <%= f.text_field :name %>

      <%= f.submit '変更を保存' %>
    </h6>

  <% end %>
